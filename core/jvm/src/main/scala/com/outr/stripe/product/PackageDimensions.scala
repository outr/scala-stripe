package com.outr.stripe.product

case class PackageDimensions(height: BigDecimal,
                             length: BigDecimal,
                             weight: BigDecimal,
                             width: BigDecimal)